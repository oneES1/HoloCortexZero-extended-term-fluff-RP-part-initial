# 2026-05-14 Queen Q8 KV 163840 context with three slots

## Scope

- Target model host: hcz workstation user service `meromero-gguf.service`.
- Persistent files changed on hcz:
  - `/path/to/services/qwen35_stack/run/start_meromero_gguf.sh`
  - `/path/to/systemd-user/meromero-gguf.service.d/10-runtime.conf`
- HCZ application container was not recreated in this step.
- Host reboot and FRP services were not touched.

## Final runtime parameters

Verified from `/proc/<pid>/cmdline` after restart:

- `-c 163840`
- `-np 3`
- `-b 384`
- `-ub 384`
- `--cache-prompt`
- `-cpent 512`
- `-ctk q8_0`
- `-ctv q8_0`
- `--mmproj /path/to/services/qwen35_stack/models-gguf/mmproj-Qwen3.6-Queen-27b-BF16.gguf`
- `--alias qwen36-queen-27b-mm-q4`

Systemd drop-in now pins the same values:

- `MEROMERO_CTX=163840`
- `MEROMERO_PARALLEL=3`
- `MEROMERO_BATCH=384`
- `MEROMERO_UBATCH=384`

## Observed state

Remote model service:

- `/health`: `{"status":"ok"}`.
- `/props`: `total_slots=3`, per-slot `n_ctx=54784`.
- GPU memory after launch and smoke tests: `22837 / 24576 MiB`.

HCZ container path:

- `http://<LOCAL_OPENAI_COMPAT_HOST>:18081/health`: `{"status":"ok"}`.
- `/props`: `total_slots=3`, per-slot `n_ctx=54784`.

Slot smoke tests from inside HCZ container:

| id_slot | elapsed | prompt_tokens | cached_tokens | completion_tokens | finish_reason |
| ---: | ---: | ---: | ---: | ---: | --- |
| 0 | 0.429s | 25 | 0 | 8 | length |
| 1 | 0.356s | 25 | 0 | 8 | length |
| 2 | 0.356s | 25 | 0 | 8 | length |

## Notes

- This supersedes the previous `102400 / np=3` context test.
- Actual VRAM growth remained lower than the earlier conservative KV estimate, but this configuration leaves only about `1.7 GiB` static headroom and should be watched under large multimodal or three-slot long-prefill bursts.
