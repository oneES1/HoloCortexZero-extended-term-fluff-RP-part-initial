# 2026-04-01 废弃配置清理

## 现象

系统设置中残留一批已无运行时引用的旧配置项，容易误导运维侧继续配置，也会增加配置模型和设置页的噪音。

## 根因

以下配置项仅在 `holo_cortex_zero/core/config.py` 中定义，仓库内不存在任何非定义引用，属于历史残留：

- `AI_SCRIPT_MAX_RETRY_TIMES`
- `AI_CHAT_LLM_API_MAX_RETRIES`
- `AI_GENERATE_TIMEOUT`
- `SESSION_ENABLE_FAILED_LLM_FEEDBACK`
- `AI_CONTEXT_LENGTH_PER_MESSAGE`
- `AI_CONTEXT_LENGTH_PER_SESSION`
- `AI_VISION_IMAGE_LIMIT`
- `AI_VISION_IMAGE_SIZE_LIMIT_KB`
- `AI_SYSTEM_NOTIFY_WINDOW_SIZE`
- `AI_SYSTEM_NOTIFY_LIMIT`
- `AI_ALWAYS_INCLUDE_MSG_ID`
- `AI_SHOW_REMOTE_URL`

## 修复

仅做最小清理：

- 从 `holo_cortex_zero/core/config.py` 删除上述 12 个废弃配置定义
- 未增加任何新逻辑
- 未修改任何仍有运行时引用的配置项
- 未改动业务主干、适配器主干、会话主干或 Docker 配置

## 验证

- `cd /path/to/source-root && rg -n --hidden --glob '!**/.git/**' --glob '!**/node_modules/**' '\b(AI_SCRIPT_MAX_RETRY_TIMES|AI_CHAT_LLM_API_MAX_RETRIES|AI_GENERATE_TIMEOUT|SESSION_ENABLE_FAILED_LLM_FEEDBACK|AI_CONTEXT_LENGTH_PER_MESSAGE|AI_CONTEXT_LENGTH_PER_SESSION|AI_VISION_IMAGE_LIMIT|AI_VISION_IMAGE_SIZE_LIMIT_KB|AI_SYSTEM_NOTIFY_WINDOW_SIZE|AI_SYSTEM_NOTIFY_LIMIT|AI_ALWAYS_INCLUDE_MSG_ID|AI_SHOW_REMOTE_URL)\b' .`
- 预期仅剩 0 处命中

## 回滚点

本次清理应独立提交，若后续确认外部配置面板仍依赖这些字段，可直接整体回退本次提交。
