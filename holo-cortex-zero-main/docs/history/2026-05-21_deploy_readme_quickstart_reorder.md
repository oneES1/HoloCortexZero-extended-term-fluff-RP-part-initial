# Deploy README Quickstart Reorder

## 时间

2026-05-21

## 背景

Docker 部署教程原有信息完整，但首次部署、国内构建源、目录职责、安装脚本说明分散，读者需要先理解较多背景才能抓到执行主线。

## 修改

- 将 `docs/README_DEPLOY.md` 重排为“最短流程 -> 目录规则 -> 模型配置 -> 可选服务 -> 国内构建源 -> 安装脚本 -> 更新/备份/排查”。
- 将国内构建源 `install.sh cn` 开关前置到首次启动命令旁，手填镜像源降级为手动方式。
- 补充安装脚本会把 `HCZ_DATA_DIR` 写成部署目录下 `data` 的绝对路径，避免相对路径说明与脚本行为混淆。
- 保留安装脚本、手动 Compose、默认配置/emoji 初始化、NapCat、备份和排查说明。

## 验证

- 文档仅重排和精简表述，未修改部署脚本、Compose 或 Dockerfile。
