# 2026-04-01 isolate 工具改名

## 目标
- 将公开 Tool 名 `block_full` 改成更温和的单词名
- 保持现有“临时隔离”逻辑不变，只改公开命名

## 本次修改
- `tool_runtime/tools/block.py`：将公开 Tool ID 从 `block_full` 改为 `isolate`
- `tool_runtime/tools/block.py`：将展示名从“屏蔽用户”改为“隔离”
- `tool_runtime/tools/__init__.py`：同步导出名改为 `isolate`
- `holo_cortex_zero/services/tools/migrated/__init__.py`：同步注册处理函数与注册日志
- `docs/guides/tool-integration.md`：同步公开 Tool 清单

## 影响分析
- Bot / 模型侧后续应调用 `isolate`，不再调用 `block_full`
- 实际底层写入仍是 `full_block`，因此数据库字段与拦截主干不变
- 这次只改公开命名，不额外增加兼容别名，避免并行主干

## 验证
- `python3 -m py_compile tool_runtime/tools/block.py tool_runtime/tools/__init__.py holo_cortex_zero/services/tools/migrated/__init__.py`
- `rg -n "block_full|isolate" tool_runtime holo_cortex_zero docs/guides`

## 回滚点
- `230cf94` 改名前快照
